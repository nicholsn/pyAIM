## pyAIM - Annotation Image Markup Python Library

pyAIM is an example web service that exposes the contents of an annotated neuroimaging dataset (i.e., NIfTI files) as XML using the Annotation Image Markup syntax.

NOTE: this is just an example and not meant for production use.
